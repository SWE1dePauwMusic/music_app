{
  "scripts": {
    "client": "cd frontend && npm run dev",
    "server": "cd backend && npm run start",
    "dev": "dotenv -e .env netlify dev"
  },
  "devDependencies": {
    "vite": "^5.3.5"
  }
}
